import './header.scss'
import Typed from 'typed.js';

let type = new Typed('.typed', {
    strings: ['KUZNETCOV', 'DREAM', 'KUZNETCOV'],
    typeSpeed: 50,
    backSpeed: 40,
    smartBackspace: true,
    showCursor: false
});

let type_team = new Typed('.typed-team', {
    strings: ['','.TEAM'],
    typeSpeed: 50,
    backSpeed: 10,
    smartBackspace: true,
    showCursor: false
});